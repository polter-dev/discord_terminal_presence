# Context ledger index

Start here, then read the module entry before opening source.

| Module | Context | Source | Purpose |
| --- | --- | --- | --- |
| CLI | [`cli.md`](cli.md) | `cmd/termp`, `install.sh`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `.github/workflows/verify-release-secrets.yml`, `.goreleaser.yaml` | Owns commands, daemon lifecycle and bounded status/config reads, an overall stop deadline that also bounds autostart discovery (#515) and shares the remaining wait across distinct daemon targets (#529), an honest indeterminate-autostart outcome on stop instead of a changed-ownership error (#534), ownership-safe Windows stderr rebinding with `*os.File` close-state coverage (#514), parsed full-uninstall dispatch results (#516), record-bound daemon executable identity across binary moves with duplicate-start refusal and working cross-path status/stop recovery (#476), live-watch reuse of a fresh validated Discord publisher when the PID file is unavailable so Windows does not misreport a daemon-owned connection as undetected (#509), honest manual update completion plus a status-visible automatic-update notice when a live daemon still needs stop and start (#584), self-healing removal of an unparseable
PID file on stop instead of surfacing a raw parser error (#491), a fail-closed (not
fail-open) generic-install writability preflight before any unattended automatic update so
a non-EACCES-unwritable destination such as a read-only mount cannot reach `install.sh`'s
`sudo` escalation branch (#495), package-wide HOME/runtime/cache isolation plus a
test-only command-signal containment guard so CLI tests cannot reach a live user daemon
(#590), a detached-start stability window so a child that dies
during `run()` initialization right after publishing its PID file is reported as a failed
start rather than a false "started in the background" success (#490), Windows detached starts that bypass invocation shims and combine `CREATE_NO_WINDOW` with the existing detached flags (#508, #510), a self-releasing no-launcher Task Scheduler fallback with readable Windows paths and default lifecycle logs (#545, #546), real-TTY interaction gates, destructive-editor busy-config refusal, invalid-config startup/reload surfacing with a self-healing config watcher, a survivable `settings` command that opens against fail-closed defaults with a naming banner instead of exiting on an unloadable config (#475), outgoing directory-privacy payload enforcement (including `directory_basename_only` at the payload boundary, #484), publication-rejection reporting, bounded detached and autostart file logs (Linux autostart uses journald), setup wiring, bounded-network updates that retire a recorded automatic-update failure once the release source is no longer offering its target (#458), a deny-list update alert gated on a TTY stderr and fed by a daemon cache refresh that runs whether or not `auto_update` is on (#457) and repeats on a ticker for the daemon's lifetime so the cache cannot go stale under it (#460), with both update-check opt-outs (`update_check = false` and a present `NO_UPDATE_CHECK`) making that whole path inert: no lookup and no state write, including no stale-record retirement (#463), full uninstall that removes only recognized termp-owned filenames from its namespace directories rather than recursively deleting whatever else lives there (#562), a daemon log opened without following a preexisting symlink and tightened to 0600 (and its directory to 0700) instead of preserving a permissive existing mode (#561), a single oversized log record bounded so it cannot itself defeat the rotation size cap (#563), platform daemon-signal functions that bind the PID record's recorded start time to the exact process handle or pidfd being signaled rather than a fresh independent snapshot (#560), static analysis, deb/rpm/dnf/openSUSE package integration coverage, and gated Homebrew/Scoop publication. |
| Completion install | [`completioninstall.md`](completioninstall.md) | `internal/completioninstall` | Installs and removes generated completions for bash, zsh, and fish. |
| Config | [`config.md`](config.md) | `internal/config` | Defines, validates (including Discord field bounds, rejecting blank directory-allowlist entries and warning on a present-but-empty top-level allowlist), migrates, performs deterministically tested bounded settled reads with a distinguishable destructive-load busy error and horizon-safe writeback defaults plus explicit read-only/unsettled opt-outs, watches with ordered reload results plus partial-snapshot protection, including treating undecodable TOML bytes as an in-flight write so a torn save is waited out instead of surfacing a parse error (#462), while a genuinely broken config still errors inside the settle budget, an O(n) bracket-nesting depth guard that fails closed on pathologically deep-but-valid TOML before ever calling the O(n^2) decoder (#497), with both multiline-string exits handling legal trailing quote runs so a crafted four-quote prefix cannot reopen a phantom string and bypass the guard (#574), and a resolved-posture loosening guard covering every privacy-affecting setting, including `display.collection` and `display.small_image` (#573), plus effective per-tool allowlist path coverage for non-atomic writes (#518) with once-per-entry canonicalization (#528), initializes, fail-closes invalid existing files, refuses to open a non-regular file (e.g. a FIFO) at the config path on every settled-read entry point instead of blocking forever, while following symlinks so a symlinked config (e.g. a dotfiles-repo setup) keeps working (#576), fail-closes manager construction on any snapshot that did not both settle and explicitly define `enabled` so a non-atomic save cannot seed presence on and leave the loosening guard unarmed (#548), returns the settle verdict from read-only loads so a caller can tell the saved config from the newest uncertified snapshot instead of silently reporting one as the other (#552), ends the manager lifecycle with an idempotent `Close` that stops the pending loosening retry without resolving it in either direction (#553), exposes a per-field duration validator (`ValidateDurationField`) over a shared zero-value policy so callers reject a bad value at entry with the same rules load enforces (#475), and saves user configuration while preserving authored allowlist paths and explicit empty per-tool allowlist overrides, with hermetic cross-platform home-expansion coverage. |
| Detector | [`detector.md`](detector.md) | `internal/detector` | Scans processes, applies terminal-activity rules and an owner-effective-UID/token-SID gate (fail-closed) that now short-circuits TTY/tmux/atime resolution (and on Windows, a spawned console-probe child) once a process is known not to be owned (#566), bounds process identity fields fed to the registry matcher against an unbounded attacker-controlled argv (#565), cross-checks process creation time between identity capture, enrichment, and ownership to mitigate PID-reuse identity mixing (#569), bounds the episode-store file/entry count and distinguishes an absent state file from a corrupt one so a caller cannot silently save an empty store over real history (#567), deleted the dead-end `ActiveDetection` helpers that always returned no detection against real scans (#568) and a Windows console-peer helper with no live caller and unconfirmed intent (#570), preserves valid per-process CPU baselines across sampling gaps and available sibling activity when another sample fails (#517, #526), launches Windows console probes windowless from the real absolute binary rather than a Scoop shim (#508), and selects the featured and other present tools. |
| Presence | [`presence.md`](presence.md) | `internal/presence` | Maps detections to activity and, at one choke point before the wire payload, sanitizes every Discord-facing text field and then bounds it so validation holds by construction, reports optional-field omissions for caller-gated diagnostics, classifies payload versus transport IPC failures, classifies each failed Unix IPC candidate by what it actually establishes — a leftover socket (`ECONNREFUSED`) or a non-socket file named `discord-ipc-N` means "not running", while a foreign-UID socket, a timeout, `EACCES`, a connect that then failed validation, or an indeterminate validation error still mean "unreachable" (#468) — reports connection health and publication (last-publish) health through separate hooks, on Windows opens the named-pipe server process exactly once and shares that handle across the SID and image-name peer checks so a PID reused between two separate opens cannot desync them (#572, Windows-unverified), and with `display.tool_name` off suppresses the tool's identity from the activity name, large image, and small image, not only details/state, falling back to the app's own name and letting Discord's own icon stand in for the cleared large image (#564). |
| Registry | [`registry.md`](registry.md) | `internal/registry` | Defines built-in/custom tools, validates custom match/exclude regex syntax and Discord-facing fields (including rejecting control/C1/bidi/invisible-formatting characters, named by codepoint, in display names, image keys, button labels, and — since #444 — every image and button URL), bounds and validates user regexes through one shared compile helper so an overlong or wrapper-closing value cannot reach a match (#577), resolves logos including an escaped LobeHub icon slug (#575), and matches process identity to tools. |
| Service | [`service.md`](service.md) | `internal/service`, `cmd/termpw` | Manages per-OS login services with rollback of failed post-write activation (#519), fail-closed rollback with actionable warnings for unknown prior macOS or Linux activation state, and surfaced indeterminate Windows rollback termination (#527), with per-dimension Linux restore, stop-gated Unix reactivation, a kept macOS definition after a failed rollback unload, and a warned-about replaced Windows task (#539), including best-effort Windows snapshots that never block install or delete a pre-existing unsnapshotted task, actionable executable-path validation with a true forced-install bypass that refuses a missing path with a named path and next command while no longer aborting on an otherwise-unresolvable one (#472) and compares the temp root in both its raw and symlink-resolved spellings so the guard also fires where `TMP` is an 8.3 short path (#542), canonical Windows task ownership (accepting the sibling `termpw.exe` companion launcher shipped for #473), an actionable and self-releasing missing-launcher fallback with a readable launcher path (#545, #546), Scoop autostart that bypasses the console shim for the real GUI launcher (#510) while persisting the stable `current` path rather than a junction-followed versioned one (#502), bounded locale-independent status queries that read the running sentinel from the fixed Last Run Result column only (#504), an ownership check that now treats an unparseable definition, an unresolved running executable, and a failed Windows task query or invalid task XML as unverified ownership requiring `--force` instead of silently passing as owned (#556, #557), `Disable`/`Enable` gated on `ForeignTask` alone so indeterminate state no longer blocks turning presence off while still permitting an unforced delete or rewrite (#556), and a `WaitDelay` on `ExecRunner.RunContext` so a service-manager command with a lingering grandchild holding its output pipe cannot keep a bounded status query blocked past its context deadline, with a killed/failed post-deadline command now returning `ctx.Err()` instead of `signal: killed` (#558). |
| Terminal text | [`terminaltext.md`](terminaltext.md) | `internal/terminaltext` | Conservatively sanitizes externally derived text with bounded handling of unterminated sequences and safely flattens multi-line values, and rejects/strips a deliberately settled invisible-Unicode-formatting rune class (Cf, Zl/Zp, plus three explicit exceptions, deliberately excluding ZWJ/ZWNJ so emoji and Persian text stay unaffected) alongside C0/C1 controls and bidi formatting controls (#571). |
| TUI | [`tui.md`](tui.md) | `internal/tui` | Owns setup with compensating cleanup after a failed new autostart install that actually wrote a definition (#519, #540), dirty-aware settings exit and footer guidance, in-place rejection of invalid duration edits and a recovered-config notice banner (#475), confirmation, watch, and card UI with shared safe rendering. |
| Update | [`update.md`](update.md) | `internal/update` | Checks releases (memoized per process for CLI runs, or repeatable via `Refresh` for the long-lived daemon), persists automatic-update outcomes including the success record used for pending-restart status (#584) and exposes the last known latest version and semver equality the CLI uses to retire them, exposes the `NO_UPDATE_CHECK` opt-out (`DisabledByEnv`) for callers with non-lookup work to skip, detects generic/Homebrew/Scoop/Go/system-package ownership with Go ownership limited to direct children of recognized bin directories (#520) compared by directory identity so a case-sensitive volume cannot alias two differently-cased bin directories (#530), with an unstattable candidate directory now treated as not the same directory rather than falling back to a case-insensitive match, closing the same #520 shape for a configured-but-not-yet-created `GOBIN` or `$GOPATH/bin` (#559), and selects safe, real-TTY-gated, network-bounded update guidance or execution. |
| Usage | [`usage.md`](usage.md) | `internal/usage` | Stores bounded local tool-usage history for settings ranking, with `Load` now distinguishing an absent state file (nil error) from a corrupt/oversize/unreadable one (non-nil error) so a caller cannot mistake the two and silently save an empty store over real data (#567). |

Platform-specific test contracts live with their module entries: filesystem-backed
launchd/systemd and Linux mount semantics run only on their native OS. Windows terminal
presence is covered by dedicated Windows tests; five Unix-specific TTY-atime/tmux fixture
tests remain skipped on Windows.

Package-manager setup guidance, including Homebrew's pre-install caveat ordering and
the non-TTY rendering safety contract, lives in the CLI module entry.
