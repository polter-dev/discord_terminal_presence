# ─────────────────────────────────────────────────────────────
# Internal build harness & design docs — kept LOCAL, never pushed.
# Agents still read these on disk; they are intentionally excluded
# from the public repository.
# ─────────────────────────────────────────────────────────────
/docs/
/AGENTS.md
/CLAUDE.md
/ROADMAP.md
/.claude/
/HANDOFF-*.md
/.obsidian/
# landing_page belongs to the separate termp-web repo; never commit here
/landing_page/

# ── Go build artifacts ───────────────────────────────────────
# Compiled binary (single-binary project) and Go tooling output
/termpresence
/termp
/dist/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
*.prof
/coverage.*

# ── Local dev / runtime ──────────────────────────────────────
# Personal manual-test scratchpad + feedback — never pushed.
/forDev/
*.pid
*.log
*.local
.env
.env.*

# ── Editor / OS cruft ────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~
